Function LastRow(wsheet As String, col As String) As Long 'wsheet is sheet name, col is column (eg "A")
Dim ws As Worksheet
Set ws = ThisWorkbook.Sheets(wsheet)

    With ws
        LastRow = .Cells(.Rows.Count, col).End(xlUp).Row
    End With

End Function
