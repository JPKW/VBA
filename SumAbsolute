Function SumAbs(Rng As Range) As Long 'this is useful for summing the absolute values of a range (eg for weighted scoring)

result = 0

    On Error Resume Next
    For Each element In Rng
        result = result + Abs(element)
    Next element
    On Error GoTo 0

SumAbs = result

End Function
